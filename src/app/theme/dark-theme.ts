import { Theme } from './symbols';

export const darkTheme: Theme = {
  name: 'dark',
  properties: {
    '--background': 'green',
    '--on-background': '#fff',
    '--primary': 'darkorange',
    '--on-primary': '#fff',
  }
} 